import React from "react";
import PageTitle from "../../components/PageTitle";

import { Container, Header, Title, Letter } from "./styles";

function work() {
  return (
    <Container>
      <PageTitle title="work" stretchedLetter="r" overlayTitle="jobs | projects | frelances" />
    </Container>
  );
}

export default work;
